# Configurações do servidor
PORT=3000
NODE_ENV=development

# Segredo JWT — troque por uma string longa e aleatória em produção!
JWT_SECRET=bgm_captacao_mude_esta_chave_em_producao_2024

# URL do frontend (deixe * para desenvolvimento local)
FRONTEND_URL=*
